plugins {
  id 'build.jar'
}

description = "gRPC Spring Boot Starters for web: gRPC for Spring Boot Web."

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api project(":grpc-spring-boot-web")
}